# Supabase (PostgreSQL)
#
# [推奨] マイグレーション (prisma migrate dev, db push) を実行する際は、ハングを避けるために「Direct connection」(ポート5432) を使用してください。
# プロジェクトの「設定」>「データベース」>「Connection string」>「Direct connection」
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres"
#
# アプリケーションの通常実行時は、コネクションプーリングを有効にするために「URI」(ポート6543) と `?pgbouncer=true` の使用が推奨されます。
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:6543/postgres?pgbouncer=true"

# Supabase Auth
# プロジェクトの「設定」>「API」で確認
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Cloudflare R2 Public URL
# R2バケットの「設定」>「パブリックアクセス」で確認できるURL
NEXT_PUBLIC_R2_PUBLIC_URL="YOUR_R2_PUBLIC_URL"

# Cloudflare R2 Bucket Name
R2_BUCKET_NAME="YOUR_R2_BUCKET_NAME"

# Cloudflare R2 API (for uploads, server-side)
# 概要ページで確認
R2_ACCOUNT_ID="YOUR_R2_ACCOUNT_ID"
# R2 > トークンを管理 > APIトークンの作成
R2_ACCESS_KEY_ID="YOUR_R2_ACCESS_KEY_ID"
R2_SECRET_ACCESS_KEY="YOUR_R2_SECRET_ACCESS_KEY"
